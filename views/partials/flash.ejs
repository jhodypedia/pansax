<% if (flash) { %>
<script>
  toastr.options = {
    closeButton: true,
    progressBar: true,
    positionClass: "toast-bottom-right",
    timeOut: 3000
  };
  toastr["<%= flash.type %>"]("<%= flash.msg %>");
</script>
<% } %>
